# 核验表单精简验证

日期：2026-09-08。页面：http://127.0.0.1:3017/ → 数据一致性核验。

## 本轮调整

- 核验范围规则移入默认收起的「更多设置」，收起时仍显示当前范围。现有选项为根类型全部对象、PLM 分类/业务范围；没有新增业务范围类型。新方案沿用全部对象默认值，已有方案保留原值。
- 发起核验窗口将大块只读卡片缩为根类型与范围、唯一标识、核验属性三行摘要；字段编码可悬停查看，历史任务快照逻辑不变。
- 固定核验属性直接在多选框中显示可移除标签，取消重复的已选明细行；每个属性的比较规则收起后按需查看。
- 样式沿用现有界面 Token 和局部作用域。

## 验证结果

- TypeScript 检查通过；19 项现有测试通过；Vite 构建通过；git diff --check 通过。构建仍有既有的大于 500 kB 资源体积提示。
- 浏览器验证文档类型方案的新建、两项属性选择、标签移除和重新选择、比较规则展开、保存及发起。
- 在 1024×768、1280×720、1440×900 窗口检查下拉框定位与边界，均正常。
- 将方案保存为 PLM 分类/业务范围后重新编辑，仅修改名称并保存，范围值仍为 PLM_SCOPE。三种运行方式均保持禁止发起；明确改回全部对象后抽样核验可发起。
- 本轮浏览器控制台无错误或警告。

## 截图

- 01-compact-plan.png：标签式属性选择、默认收起的规则和范围设置。
- 02-compact-run.png：精简后的发起窗口。
- 03-scope-preserved.png：已有分类范围保持不变，运行限制仍有效。
- 04-run-result.png：全部对象方案发起后的页面。

## 当前边界

项目仍为本地原型；PLM 分类范围尚未接入，相关执行限制保留。本轮没有更改核验、统计和历史快照语义，也未推送远端。
