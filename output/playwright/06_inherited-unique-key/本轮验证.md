# 唯一标识自动沿用

2026-09-08，基于 486ab0e。

- 移除新建、编辑核验方案里的「唯一标识属性」下拉框；表单校验与保存时自动读取所选根类型的正式唯一标识配置。
- 保留方案和运行快照中的唯一标识字段、配置缺失/冲突校验及唯一键不参与普通字段比对的规则。
- 旧方案的唯一标识与当前配置不一致时，提示编辑并保存方案更新，避免要求用户操作已移除的下拉框。
- 浏览器验证：新建 Part 方案无需再选唯一键即可保存；编辑为 Document 后清空旧比较属性，重新选择后自动沿用文档编号；ID 核验正常完成，展开历史只读快照可见 iba_doc_number；Process 缺少正式唯一键时仍禁止保存并显示原因。
- TypeScript、23 项既有测试、构建通过。构建仍有既有资源体积警告。
- 截图：01-create-plan.png、02-task-snapshot.png。

其他范围选择、执行和统计逻辑没有改动。
